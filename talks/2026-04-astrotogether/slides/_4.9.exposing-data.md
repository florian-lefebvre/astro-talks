# <span class="text-gradient font-normal">Exposing</span> data

```ts
// vite-plugin-fonts.ts
load(id) {
	if (id === '\0virtual:astro:assets/fonts/internal') {
		return `
			export const componentDataByCssVariable = new Map(
                ${JSON.stringify(Array.from(componentDataByCssVariable.entries()))}
            );
			export const fontDataByCssVariable = ${JSON.stringify(fontDataByCssVariable)};
		`;
	}
}
```

```astro
---
// Font.astro
import { componentDataByCssVariable } from 'virtual:astro:assets/fonts/internal';

const { cssVariable, preload = false } = Astro.props as Props;
const data = componentDataByCssVariable.get(cssVariable);
---
<style set:html={data.css}></style>
```
